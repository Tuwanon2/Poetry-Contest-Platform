# Database settings used by the app and DB service
APP_PORT=8080

# Use external DB (running in your other Docker / expose host port 5435)
# When DB runs in a different docker-compose, expose its port on the host (e.g. 5435)
# and point app to host.docker.internal so container can reach the host service.
POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=5435
POSTGRES_USER=klon_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=poem_competition
POSTGRES_DBNAME=poem_competition
POSTGRES_SSLMODE=disable
